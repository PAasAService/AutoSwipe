/** Used before “חזור” / “חזרה” in labeled back rows (‹). */
export const BACK_ICON = '\u2039' // ‹

/** Paired with “הבא” / forward CTAs where a single arrow glyph is used. */
export const FORWARD_ICON = '\u203a' // ›

/** Icon-only back chevron — points right (‹ is left). */
export const BACK_ICON_ONLY = '\u203a' // ›

/** Chevron-only back (no “חזור”) — slightly larger than body text. */
export const BACK_ICON_ONLY_SIZE = 22

/** `‹ חזור` / `‹ חזרה` — same size as typical screen titles (see headers in app). */
export const BACK_WITH_LABEL_FONT_SIZE = 18

/** Header dismiss: סגור / ביטול (secondary, not the chevron row). */
export const HEADER_DISMISS_FONT_SIZE = 16
